apiVersion: v1
kind: ConfigMap
metadata:
  name: rhcsa-15-scheduling-at-cron-systemd-timers-lab
  labels:
    app: girus-lab-template
    lab: rhcsa-15-scheduling-at-cron-systemd-timers
data:
  lab.yaml: |
    name: rhcsa-15-scheduling-at-cron-systemd-timers
    title: 'RHCSA 15: at, cron e systemd timers'
    description: Modele agendamentos com at, cron e systemd timers conforme objetivos EX200.
    duration: 35m
    image: linuxtips/girus-devops:0.1
    type: linux
    tags:
    - RHCSA
    - Linux
    - RHEL
    - blcsilva
    - Automacao
    timerEnabled: true
    tasks:
    - name: 'Conhecimento aplicado: at, cron e systemd timers'
      description: Modele agendamentos com at, cron e systemd timers conforme objetivos EX200. Este lab incorpora adendos EX200 RHEL 10 mantendo o formato GIRUS atual.
      steps:
      - '**Proposta do lab:** Modele tarefas recorrentes e diferencie cron de systemd timers em RHEL. Este exercicio pertence ao bloco RH134, alinhado a administracao avancada: automacao, logs, SELinux, storage, boot, seguranca de rede e containers.'
      - '**Contexto de aplicacao:** no RHCSA voce precisa resolver tarefas no terminal, produzir evidencias rapidas e reconhecer quando um recurso depende do ambiente RHEL completo. Neste GIRUS, comandos destrutivos de disco, boot ou servico real sao tratados como roteiro controlado quando o container nao oferece o subsistema.'
      - '**Referencia oficial:** RH134 - https://www.redhat.com/en/services/training/rh134-red-hat-system-administration-ii'
      - '**Cenario pratico:** imagine que voce recebeu um chamado ou tarefa de prova envolvendo modele tarefas recorrentes e diferencie cron de systemd timers em rhel. O objetivo nao e decorar uma linha de comando, e reconhecer o estado inicial, executar a menor mudanca necessaria e provar o resultado.'
      - '**Dominio RHCSA trabalhado:** Automacao. Automacao reduz erro repetitivo. Scripts, regex e agendamento devem ter entrada, saida e codigo de retorno previsiveis.'
      - '**Ligacao com o treinamento oficial:** Este bloco aprofunda operacao real: automacao, troubleshooting, seguranca, storage, boot, rede avancada e containers com foco em resolver problemas no terminal.'
      - '**Criterio de sucesso antes de executar:** ao final, a validacao automatica precisa retornar `ok`. Leia a validacao agora para entender a evidencia esperada: `grep -q ''OnCalendar'' /tmp/rhcsa-15/rhcsa.timer && grep -q ''/usr/local/bin/rhcsa-check.sh'' /tmp/rhcsa-15/rhcsa.cron && echo ok`.'
      - '**Mapa dos comandos e parametros:** `mkdir -p` cria a arvore completa e nao falha se o diretorio ja existir. `printf` gera conteudo previsivel e evita ambiguidades de `echo` entre shells. `cat` exibe conteudo; com `tee`, tambem grava a evidencia em arquivo.'
      - '**Aplicacao em RHEL real:** Use para transformar sequencias manuais em tarefas repetiveis com saida previsivel.'
      - '**Erros comuns para evitar:** executar comando no diretorio errado, nao validar o arquivo gerado, confundir saida exibida com estado persistido, ignorar mensagens de erro e seguir para o proximo passo sem evidencia.'
      - '**Checklist mental:** 1) qual recurso sera alterado? 2) qual comando mostra o estado atual? 3) qual comando aplica a mudanca? 4) qual saida prova que funcionou? 5) como eu desfaria ou repetiria com seguranca?'
      - '**Skill IDs EX200:** RHCSA-AT, RHCSA-CRONTAB, RHCSA-ETC-CRON, RHCSA-SYSTEMD-TIMER.'
      - '**Nivel da skill:** Skill Guided. Guided permite copiar e entender; Challenge exige escolher comandos; Exam entrega cenario e validacao com minima ajuda.'
      - '**Fases de aprendizagem:** FASE 1 - Descoberta: levante estado atual. FASE 2 - Execucao: aplique a menor mudanca necessaria. FASE 3 - Validacao: use a validacao automatica e comandos de conferencia. FASE 4 - Reforco: altere mentalmente um requisito e explique como adaptaria a solucao.'
      - '**Estado desejado ampliado:** Representar agendamento pontual com at, recorrente com cron e systemd timer.'
      - '**Cobertura EX200 reforcada:** Dominio D7 - Deploy & Maintain. at, cron e systemd timer units.'
      - '**Referencia EX200:** objetivos oficiais do Red Hat Certified System Administrator exam em RHEL 10 - https://www.redhat.com/en/services/training/ex200-red-hat-certified-system-administrator-rhcsa-exam'
      - '**Como ler os comandos:** opcoes curtas como `-p`, `-c`, `-f` ajustam comportamento imediato; opcoes longas como `--help`, `--version`, `--permanent` explicam intencao. Redirecionamentos como `>` gravam a saida, `| tee` grava e exibe, e `2>/dev/null` silencia erros esperados de ferramentas ausentes no container.'
      - '**Pratica orientada:** execute os comandos abaixo na ordem e observe a saida. Quando aparecer `RHEL real`, trate como referencia de prova para executar em uma VM RHEL completa.'
      - mkdir -p /tmp/rhcsa-15
      - 'printf ''*/5 * * * * /usr/local/bin/rhcsa-check.sh

        '' > /tmp/rhcsa-15/rhcsa.cron'
      - 'printf ''[Timer]

        OnCalendar=*:0/5

        [Install]

        WantedBy=timers.target

        '' > /tmp/rhcsa-15/rhcsa.timer'
      - cat /tmp/rhcsa-15/rhcsa.cron /tmp/rhcsa-15/rhcsa.timer
      - '**Conclusao e reforco:** voce praticou agendamento com cron e timers no dominio automacao. Antes de seguir, diga em voz alta qual arquivo, comando ou saida provou que a tarefa foi concluida; esse habito reduz erro em prova pratica.'
      tips:
      - type: tip
        title: Ritmo RHCSA
        content: Treine primeiro entendendo o comando; depois repita buscando velocidade, clareza e validacao objetiva.
      validation:
      - command: grep -q 'OnCalendar' /tmp/rhcsa-15/rhcsa.timer && grep -q '/usr/local/bin/rhcsa-check.sh' /tmp/rhcsa-15/rhcsa.cron && echo ok
        expectedOutput: ok
        errorMessage: Os exemplos de cron/timer nao foram criados corretamente.
